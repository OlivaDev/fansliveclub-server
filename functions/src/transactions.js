const admin = require("firebase-admin");
const cors = require("cors");
const corsHandler = cors({ origin: "*" })


const sendGift = onRequest(async (req, res) => {
    corsHandler(req, res, async () => {
        
    })
})